<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 获取ip地址 -->
    <define name="ip" class="com.coco.boot.config.core.CustomizedApplicationIP"/>

    <conversionRule conversionWord="remoteIp" converterClass="com.coco.boot.config.core.CustomizedRemoteIp" />
    <!-- 从Spring中获取应用名称 -->
    <springProperty scope="context" name="applicationName" source="spring.application.name"/>
    <!-- 从Spring中获取应用端口号 -->
    <springProperty scope="context" name="port" source="server.port"/>
    <!-- 日志存放路径 -->
    <springProperty scope="context" name="log.path" source="log.path"/>
    <!-- 日志最大的历史 -->
    <springProperty scope="context" name="maxHistory" source="log.max-history"/>
    <!-- 日志输出格式 -->
    <property name="log.pattern"
              value="[${applicationName}, ${ip}:${port}, %remoteIp] %-5p %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c.%M:%L - %m%n"/>
    <!-- 彩色日志格式 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="[${applicationName} ${ip}:${port} %remoteIp] ${CONSOLE_LOG_PATTERN:-%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <!-- 彩色日志依赖的渲染类 -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>
<!--    [boot, 192.168.192.2:8181, 127.0.0.1] INFO  2024-03-21 14:40:56.640 [main] com.coco.boot.CocoBootApplication.logStarted:57 - Started CocoBootApplication in 5.637 seconds (process running for 7.146)-->

<!--    2024-03-21 14:41:36.604  INFO 28544 -&#45;&#45; [           main] com.coco.boot.CocoBootApplication        : Started CocoBootApplication in 5.735 seconds (process running for 7.183)-->

    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    <!-- 系统日志输出 -->
    <appender name="file_info" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/info.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/%d{yyyy-MM-dd}/info.log.gz</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
    </appender>

    <appender name="file_error" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/error.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/%d{yyyy-MM-dd}/error.log.gz</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 过滤的级别 -->
            <level>ERROR</level>
            <!-- 匹配时的操作：接收（记录） -->
            <onMatch>ACCEPT</onMatch>
            <!-- 不匹配时的操作：拒绝（不记录） -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>
    <appender name="file_redis" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/redis.log</file>
        <!-- 循环政策：基于时间创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志文件名格式 -->
            <fileNamePattern>${log.path}/%d{yyyy-MM-dd}/redis.log.gz</fileNamePattern>
            <!-- 日志最大的历史 60天 -->
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
    </appender>
    <logger name="redis" level="INFO" additivity="false">
        <appender-ref ref="file_redis"/>
    </logger>
    <!-- 系统模块日志级别控制  -->
    <logger name="com.coco.boot" level="info"/>
    <!-- Spring日志级别控制  -->
    <logger name="org.springframework" level="warn"/>
    <root level="info">
        <appender-ref ref="console"/>
    </root>

    <!--系统操作日志-->
    <root level="info">
        <appender-ref ref="file_info"/>
        <appender-ref ref="file_error"/>
    </root>
</configuration>
